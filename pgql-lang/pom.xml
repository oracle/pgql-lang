<?xml version="1.0" encoding="UTF-8"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd" xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <modelVersion>4.0.0</modelVersion>
  <groupId>oracle.pgx</groupId>
  <artifactId>pgql-lang</artifactId>
  <version>2016.05.31</version>

  <dependencies>
    <dependency>
      <groupId>oracle.pgx</groupId>
      <artifactId>graph-query-ir</artifactId>
      <version>2016.05.31</version>
      <scope>compile</scope>
    </dependency>
    <dependency>
      <groupId>org.metaborg</groupId>
      <artifactId>spoofax</artifactId>
      <version>2.0.0-beta1</version>
      <exclusions> <!-- we don't need this -->
        <exclusion>
          <groupId>commons-beanutils</groupId>
          <artifactId>commons-beanutils</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
  </dependencies>

  <repositories>
    <!-- for now, we host Spoofax locally until we switch to a future stable build that is hosted on the Spoofax server -->
    <repository>
      <id>artifactoryRepository</id>
      <url>http://artifactory-slc.oraclecorp.com/artifactory/simple/labs-pgx-release-local/</url>
    </repository>

    <!-- not used a.t.m.
    <repository>
      <id>metaborgRepo</id>
      <url>http://artifacts.metaborg.org/content/repositories/releases/</url>
    </repository>
    -->
  </repositories>

  <properties>
    <maven.compiler.source>1.7</maven.compiler.source>
    <maven.compiler.target>1.7</maven.compiler.target>
  </properties>

  <build>
    <resources>
      <resource>
        <directory>target/spoofax-binaries</directory>
      </resource>
    </resources>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <configuration>
          <archive>
            <manifest>
              <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
            </manifest>
          </archive>
        </configuration>
      </plugin>
    </plugins>
  </build>

</project>
