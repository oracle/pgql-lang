module pgql10

language pgql-lang

test Variable definition in GROUP BY [[

  SELECT age
    FROM MATCH( (n) )
  GROUP BY n.age [[AS age]]

]] error like "Variable definition not allowed in GROUP BY" at #1
